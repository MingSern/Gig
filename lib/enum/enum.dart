enum AuthStatus {
  notSignedIn,
  signedIn,
}

enum UserType {
  jobseeker,
  employer,
}

enum JobStatus {
  pending,
  shortlisted,
  declined,
}

enum WarningLevel {
  clear,
  caution,
  danger,
}
